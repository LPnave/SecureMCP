# Secure Prompt MCP Server Environment Configuration
# Copy this file to .env and modify as needed

# Authentication Mode
# Options: static (development), jwt (production), hmac (internal services)
FASTMCP_AUTH_MODE=static

# JWT Configuration (for production mode)
# JWT_JWKS_URI=https://your-auth-provider.com/.well-known/jwks.json
# JWT_ISSUER=https://your-auth-provider.com
# JWT_AUDIENCE=secure-prompt-mcp-api

# HMAC Configuration (for internal services mode)
# JWT_SHARED_SECRET=your-32-character-minimum-shared-secret-key-here
# JWT_ISSUER=internal-auth-service
# JWT_AUDIENCE=secure-prompt-mcp-internal
# JWT_ALGORITHM=HS256

# Server Configuration
# SERVER_HOST=0.0.0.0
# SERVER_PORT=8001

# Logging Configuration
# LOG_LEVEL=INFO

# Security Configuration
# DEFAULT_SECURITY_LEVEL=medium
